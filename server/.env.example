# Email SMTP (e.g. Namecheap Private Email)
SMTP_HOST=mail.privateemail.com
SMTP_PORT=465
SMTP_USER=your@superengulfing.com
SMTP_PASS=your_password
SMTP_FROM="SuperEngulfing <info@superengulfing.com>"
# Optional: reply-to address (helps deliverability)
# SMTP_REPLY_TO=info@superengulfing.com

# IMPORTANT: For production, use your PUBLIC URLs (HTTPS). Using localhost in emails causes links to break and triggers spam filters.
API_URL=https://your-api-domain.com

# CORS: allowed origin(s) for API requests in production (comma-separated for multiple, or single origin)
# CORS_ORIGIN=https://your-site.com

# JWT: required in production - use a long random string (e.g. openssl rand -hex 32)
# JWT_SECRET=your-secret-here

# Frontend/SEO: used for sitemap.xml and robots.txt absolute URLs
# VITE_SITE_URL=https://superengulfing.com
THANK_YOU_URL=https://your-site.com/thank-you

# PDF link (e.g. Google Drive) - fallback if per-locale not set
PDF_LINK=https://drive.google.com/...

# PDF links per locale (fallback: PDF_LINK)
# PDF_LINK_EN=https://drive.google.com/file/d/.../view?usp=sharing
# PDF_LINK_AM=https://drive.google.com/file/d/1Y4yz845u2n7y8la2t9oaaieCUfnnq0A0/view?usp=sharing

# Welcome video (Wistia embed) per locale - shown on thank-you page
# WELCOME_VIDEO_EN=https://fast.wistia.net/embed/iframe/xxxxx?videoFoam=true
# WELCOME_VIDEO_AM=https://fast.wistia.net/embed/iframe/xxxxx?videoFoam=true

# Admin notifications per locale (who gets access/indicator request emails)
# Fallback: ADMIN_EMAIL
# ADMIN_EMAIL_EN=admin-en@example.com,admin2@example.com
# ADMIN_EMAIL_AM=admin-am@example.com

# Admin login per locale (who can access /admin vs /am/admin)
# Fallback: ADMIN_EMAILS (comma-separated)
# ADMIN_EMAILS_EN=admin-en@example.com
# ADMIN_EMAILS_AM=admin-am@example.com

# Admin emails: comma-separated (fallback for notifications and login)
ADMIN_EMAIL=admin1@example.com,admin2@example.com
# ADMIN_EMAILS=admin1@example.com,admin2@example.com

# Frontend URL (for set-password link and "Review in Admin" link in admin notification)
FRONTEND_URL=https://superengulfing.com

# Throttle: max emails per minute when sending broadcasts/sequences (0 = no throttle)
# THROTTLE_EMAILS_PER_MINUTE=50

# Optional: auto-add new confirmed subscribers to this sequence (ID). Leave empty to disable.
# DEFAULT_SEQUENCE_ID=1

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=superengulfing_email
DB_USER=postgres
DB_PASSWORD=...

# NOWPayments (LS3MONTHOFF + paid courses): IPN webhook and notifications
# NOWPAY_IPN_SECRET=your-ipn-secret-from-nowpayments-dashboard
# NOWPAY_NOTIFY_EMAIL=admin@example.com
# NOWPAYMENTS_COURSE_WIDGET_IID=6065193944

# USDT TRC20 self-hosted payments (LiquidityScan PRO, paid courses)
# USDT_TRC20_WALLET_ADDRESS=TYourTronWalletAddressForReceivingUSDT
